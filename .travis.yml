language: swift
osx_image: xcode11.5
os: osx
install:
  - swift package update
script:
  - swift build
  - swift test --enable-code-coverage
after_success:
  - bash <(curl -s https://codecov.io/bash)
